一定注意最大宽度和最大高度的设置！！！
假设一张240x480个像素的图片，设置为240x240取模，
则软件最终取模结果为120x240个像素点。

8位真彩 ：用1个字节表示一个像素点的颜色（256色）。
16位真彩：用2个字节表示一个像素点的颜色（65536色）。
24位真彩：用3个字节表示一个像素点的颜色（2^24色）。
32位真彩：用3个字节表示一个像素点的颜色（2^24色），并且用一个字节控制图片的透明度。